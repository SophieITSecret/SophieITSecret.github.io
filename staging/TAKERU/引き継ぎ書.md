# Claudeへの引き継ぎ書
## TAKERUプロジェクト（MSアカデミー）開発状況

作成：牧村健志
更新：2026年4月7日（第2セッション終了時点）

---

## 1. プロジェクト概要

**TAKERU（MSアカデミー）**は軍事戦略の基礎知識を普及・啓蒙するスマホ向けWebアプリです。
- 3級・2級・1級のカリキュラム構成（現在3級のみ開講）
- カード形式（図表＋テキスト）で学習
- GitHub Pagesで静的配信

**リポジトリ：** SophieITSecret.github.io
**対象フォルダ：** `staging/Takeru/`
**現在のバージョン：** v2.3（app.js）／v2.4（style.css）

---

## 2. ファイル構成

```
staging/Takeru/
├── index.html          # メイン画面（v2.3）
├── app.js              # 全機能（v2.3ベース安定版）
├── style.css           # デザイン（v2.4）
├── TAKERUcard.csv      # カードデータ
├── takeru-touch-icon.png  # PWAアイコン（180×180）
├── images/
│   ├── takeru-top.jpg  # トップメニュー画像（4:3）
│   ├── ASAKA01.png     # 各カードの画像（{コード}.png or .jpg）
│   └── ...
└── voices/
    └── {コード}.mp3    # 音声ファイル（現在は兵科6枚のみ）
```

---

## 3. CSVデータ構造（TAKERUcard.csv）

| 列 | 内容 | 例 |
|---|---|---|
| c[0] | カードID | ASAKA01 |
| c[1] | ジャンル | 駐屯地、基地ツアー |
| c[2] | セクション | 朝霞駐屯地見学ツアー |
| c[3] | タイトル | 朝霞駐屯地へようこそ！ |
| c[4] | 本文 | 360文字程度 |

現在28枚のカードがあります。

---

## 4. 完成している機能

- ✅ **5ボタントップメニュー**（受講・図書館・ニュース・リンク集・受験案内）
- ✅ **級選択メニュー**（1級・2級は「ご期待ください」、3級のみ開講）
- ✅ **3階層ナビゲーション**（ジャンル→セクション→カード）
- ✅ **4:3画像表示**（縦向き）
- ✅ **グリーン境界線**（画像とテキストの間）
- ✅ **進捗表示**（セクション名と枚数）
- ✅ **ボタン構成**（左から：🏠ホーム・☰メニュー・◀戻る・次へ▶・🔊読上）
- ✅ **戻るボタン2機能**（テキスト表示中→前のカード、メニュー表示中→上の階層）
- ✅ **ホームボタン**（一発でトップメニューへ）
- ✅ **セクション完了表示**
- ✅ **ダーク/ホワイトモード**（設定パネル、localStorageで記憶）
- ✅ **文字サイズ切り替え**（小・中・大）
- ✅ **PWA対応**（ホーム画面に追加、アイコン表示）
- ✅ **プルダウンで更新**（220px以上で発動、誤作動防止強化済み）
- ✅ **カメラ穴対応**（safe-area-inset-top）
- ✅ **MP3再生**（voices/{コード}.mp3）
- ✅ **音声ワーニング**（MP3がない場合「🔇 音声データがありません」を表示）
  - ワーニングをタップ→閉じる
  - 読上ボタンを再度タップ→TTS試行
  - カード切り替え時に自動で消える
- ✅ **縦向きテキスト左右padding**：4px（余白最小化済み）
- ✅ **縦向きPWA下の隙間**：padding-bottom:0 で解消済み
- ✅ **横向きボタン**：右端32px極細縦列、記号のみ（🏠☰◀▶🔊）
- ✅ **横向き画像75vw／テキスト25vw**：基本レイアウト実装済み
- ✅ **フォントはゴシック体**（Noto Sans JP）

---

## 5. 未解決の課題

### ⚠️ 課題1：横向きiPhone15で画像上下に隙間が残る

**症状：** iPhone15横向き時、画像の上下に少し黒い余白が残る。iPhone8では解消済み。

**原因の推測：**
- `window.innerHeight`とCSSの`100dvh`が微妙にズレている可能性
- `getBoundingClientRect().height`での取得も試みたが改善せず

**次回やること：**
- 横向きになったときに画面上にデバッグ数値を表示するロジックを入れて実測値を確認する
- 具体的には`applyLandscapeLayout()`関数内にオーバーレイ表示を追加
- 実測値（innerW・innerH・imageArea実H）を確認してから計算式を修正

**現在のロジック（app.js）：**
```javascript
// この関数はv2.3では削除されている（v2.4cで試みたが動作不良で一旦戻した）
function applyLandscapeLayout() {
    const screenH = window.innerHeight;
    const idealImageW = Math.floor(screenH * 4 / 3);
    // ...
}
```

**重要：** v2.4cでデバッグ表示を追加したところ起動不能になった。
原因はテンプレートリテラル（バッククォート）ではなく不明。
次回は小さな変更を1つずつ確認しながら進めること。

---

### ✅ 課題2（解決済み）：iPhoneでTTSが動かない

**解決策：** MP3がない場合はTTSを試みずワーニングを表示する方式に変更。
ユーザーが読上ボタンを再度押した場合のみTTSを試行。

---

## 6. style.cssの主要変更点（v2.4）

```css
/* 縦向きテキスト余白 */
#text-view {
    padding: 10px 4px;  /* 以前は14px */
}

/* 縦向きPWA下の隙間 */
body {
    padding-bottom: 0;  /* 以前はenv(safe-area-inset-bottom) */
}

/* 横向き：画像75vw固定 */
#image-area {
    width: 75vw !important;
    aspect-ratio: unset !important;
}

/* 横向き：ボタン右端32px極細縦列 */
#controls {
    width: 32px !important;
    /* CSSの::afterで記号を表示 */
}
```

---

## 7. 今後の追加予定コンテンツ

- 図書館、ニュース・お知らせ、リンク集、受験案内（現在は「準備中」表示）
- 2級・1級のカリキュラム
- 全カードの音声ファイル（VOICEPEAK生成）

---

## 8. 技術的な注意事項

**Safariのキャッシュ問題：**
SafariはChromeやBraveと異なり、キャッシュが強固。
ファイルを更新しても反映されない場合は、iPhoneの設定→Safari→「履歴とWebサイトデータを消去」で解決。

**GitHubのキャッシュ：**
URLに`?v=1`などを付けて強制リロードできる。

**localStorageのキー：**
- `takeru-theme`：dark / light
- `takeru-font`：small / medium / large

**部分的なコード差し替えは危険：**
牧村さんが直接GitHubエディターで部分差し替えをすると動作不良になることがある。
必ずフルファイルで差し替えること。

---

## 9. 開発体制

| 担当 | 役割 |
|---|---|
| 牧村さん | 総指揮・コンテンツ制作・テスト |
| Claude | TAKERUアプリの主担当開発 |
| まっちゃん（Gemini） | BarSophieの主担当開発 |

---

## 10. 音声ファイル生成方法（VOICEPEAK CLI）

TAKERUの音声ファイルはBarSophieと同じ方法で生成できます。

### 環境
- **ツール：** VOICEPEAK（Windows版）
- **インストール先：** `C:\Program Files\VOICEPEAK\`
- **ナレーター名：** `Japanese Female 1`（※`女性1`は動かない、英語名必須）
- **変換ツール：** pydub（Python）

### ハマりポイント（BarSophieで経験済み）

1. **ナレーター名は必ず英語**
2. **-sオプションを使う**（`-t`は`Internal BUG`エラーが出る）
3. **数字は読み仮名に変換**
4. **VOICEPEAKは連続処理で不安定**：`time.sleep(3.0)`は必須
5. **5本ごとに10秒休憩**
6. **スリープをオフに**
7. **TAKERUのCSVはヘッダー行あり、本文は4列目（index 4）**

### 生成スクリプト（voicepeak_takeru.py）

```python
import os, subprocess, wave, time, re
import pandas as pd

VP_DIR = r"C:\Program Files\VOICEPEAK"
VP_EXE = "voicepeak.exe"
NARRATOR = "Japanese Female 1"
EMOTION = "happy=20,fun=10"
SPEED = "100"
PITCH = "50"
CSV_FILE = os.path.abspath("TAKERUcard.csv")
OUTPUT_DIR = os.path.abspath("voices_wav")
TEMP_DIR = os.path.abspath("temp_work")

for d in [OUTPUT_DIR, TEMP_DIR]:
    if not os.path.exists(d): os.makedirs(d)

def split_text(text, max_len=40):
    parts = re.split(r'(?<=。)', text)
    result = []
    for part in parts:
        if not part.strip(): continue
        if len(part) > max_len:
            sub_parts = re.split(r'(?<=、)', part)
            buf = ""
            for sp in sub_parts:
                if len(buf) + len(sp) > max_len and buf:
                    result.append(buf); buf = sp
                else:
                    buf += sp
            if buf: result.append(buf)
        else:
            result.append(part)
    return [r for r in result if r.strip()]

def combine_wav(file_list, output_path):
    data = []
    for f in file_list:
        with wave.open(f, 'rb') as w:
            data.append([w.getparams(), w.readframes(w.getnframes())])
    with wave.open(output_path, 'wb') as out:
        out.setparams(data[0][0])
        for d in data: out.writeframes(d[1])

def generate():
    df = pd.read_csv(CSV_FILE, encoding='utf-8-sig', header=None).iloc[1:]
    processed = 0
    for _, row in df.iterrows():
        talk_id = str(row[0]).strip()
        full_text = str(row[4]).strip()
        final_output = os.path.join(OUTPUT_DIR, f"{talk_id}.wav")
        if os.path.exists(final_output): continue
        sentences = split_text(full_text)
        chunk_files = []
        print(f"処理中: {talk_id} ({len(sentences)}分割)")
        success = True
        for i, chunk in enumerate(sentences):
            chunk_file = os.path.join(TEMP_DIR, f"chunk_{talk_id}_{i}.wav")
            cmd = [os.path.join(VP_DIR, VP_EXE), "-s", chunk,
                   "-n", NARRATOR, "-e", EMOTION,
                   "--speed", SPEED, "--pitch", PITCH, "-o", chunk_file]
            result = subprocess.run(cmd, cwd=VP_DIR, capture_output=True, text=True)
            time.sleep(3.0)
            if result.returncode != 0:
                print(f"❌ エラー (ID:{talk_id}, chunk:{i}): {chunk}")
                success = False; break
            chunk_files.append(chunk_file)
        if not success:
            for f in chunk_files:
                if os.path.exists(f): os.remove(f)
            print(f"⚠️ {talk_id} スキップ")
            continue
        combine_wav(chunk_files, final_output)
        for f in chunk_files:
            if os.path.exists(f): os.remove(f)
        processed += 1
        if processed % 5 == 0:
            print(f"--- {processed}本完了。10秒休憩 ---")
            time.sleep(10)
    print("✨ 完了！")

if __name__ == "__main__":
    generate()
```

### WAV→MP3変換（wav_to_mp3.py）

```python
import os
from pydub import AudioSegment

INPUT_DIR = "voices_wav"
OUTPUT_DIR = "voices"

if not os.path.exists(OUTPUT_DIR):
    os.makedirs(OUTPUT_DIR)

files = sorted([f for f in os.listdir(INPUT_DIR) if f.endswith(".wav")])
for i, filename in enumerate(files):
    mp3_name = filename.replace(".wav", ".mp3")
    mp3_path = os.path.join(OUTPUT_DIR, mp3_name)
    if os.path.exists(mp3_path):
        print(f"[{i+1}] スキップ: {mp3_name}"); continue
    audio = AudioSegment.from_wav(os.path.join(INPUT_DIR, filename))
    audio = audio.set_channels(1)
    audio.export(mp3_path, format="mp3", bitrate="128k")
    print(f"[{i+1}] 変換完了: {mp3_name}")
print("✨ 完了！")
```

### 抜けチェック（check_missing.py）

```python
import os
import pandas as pd

df = pd.read_csv("TAKERUcard.csv", encoding='utf-8-sig', header=None).iloc[1:]
missing = [str(row[0]).strip() for _, row in df.iterrows()
           if not os.path.exists(os.path.join("voices", f"{str(row[0]).strip()}.mp3"))]
print(f"未完成: {len(missing)}本")
print("IDリスト:", missing)
```
